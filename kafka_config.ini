# Author: Eon (Himanshu Shekhar)
# Created: 2025-08-15
# Description: Example configuration file for Kafka Kerberos utility
# License: MIT
# Repository: https://github.com/eonn/kafka-python-kerberos

[kafka]
# Required Configuration
bootstrap_servers = broker1.example.com:9092,broker2.example.com:9092
topic = my-kafka-topic
keytab_path = /path/to/your/user.keytab
principal = user@EXAMPLE.COM
service_name = kafka

# Optional Configuration
consumer_group_id = kafka-utility-group
auto_offset_reset = earliest
max_poll_records = 500
session_timeout_ms = 30000
heartbeat_interval_ms = 3000

# Optional JAAS Configuration (if not provided, will be auto-generated)
# jaas_config_path = /path/to/custom/jaas.conf
